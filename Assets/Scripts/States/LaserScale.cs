using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LaserScale : MonoBehaviour
{
    /*
    private float speed = 0.5f;
    private Transform _trans;
    private bool isScaling;

    
     void Update()
     {
        if (!isScaling)    
            SpawnLaser();
     }

    IEnumerator SpawnLaser()
    {
        isScaling = true;

        if (transform.localScale.x <= _trans.transform.localScale.x * 2)
        {
            transform.localScale += (new Vector3(speed, 0, speed) * Time.deltaTime);    //Увеличение
        }
        else if (_trans.transform.localScale.x > transform.localScale.x)
        {
            transform.localScale += (new Vector3(-speed, 0, -speed) * Time.deltaTime);  //Уменьшение
        }

        isScaling = false;
        yield return new WaitForSeconds(3);
    }
    */
}